description 'Testing module for rozsa-network'

dependencies {
    implementation project(':rozsa-network')
}

jar {
    manifest { attributes 'Main-Class': 'com.rozsa.Main' }
    from { configurations.compileClasspath.collect { it.isDirectory() ? it : zipTree(it) } }
}
